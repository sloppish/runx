{
  "name": "runx-ui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "bun ui/build.ts",
    "build:release": "bun ui/build.ts --release",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test ui/"
  },
  "devDependencies": {
    "bun-types": "^1.3.13",
    "typescript": "^5.8.0"
  }
}
